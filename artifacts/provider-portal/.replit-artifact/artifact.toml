kind = "web"
previewPath = "/"
title = "SaluraCare Provider Portal"
version = "1.0.0"
id = "artifacts/provider-portal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/provider-portal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/provider-portal", "run", "build" ]
publicDir = "artifacts/provider-portal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
